# .github/workflows/release-please.yml
name: Release-please
on:
  push:
    branches:
      - dev

permissions:
  contents: write
  pull-requests: write

jobs:
  release-please:
    runs-on: ubuntu-latest
    steps:
      - uses: nikhilmakhijani/sharedworkflow/.github/workflows/release-tag.yml
